# Extension dependencies (project scope section 21).
#
# Floors, not pins: this file says what the code needs. The exact versions this
# work was verified against are in ../requirements-lock.txt, which is what to
# install if you want the published numbers to reproduce bit for bit.

# --- numerical core ------------------------------------------------------
numpy>=1.20
pandas>=1.3
scipy>=1.7
matplotlib>=3.4

# --- market data ---------------------------------------------------------
yfinance>=0.2

# --- dashboard (advanced extension phase 4) ------------------------------
# streamlit>=1.36 is the floor for the st.navigation / st.Page multipage API
# and for streamlit.testing.v1.AppTest, both of which this project uses.
streamlit>=1.36
plotly>=5.18
