# Runtime dependencies for OptionMC.
# Version floors mirror optionmc/setup.py install_requires exactly, so this file
# never drifts from what the package itself declares.
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0
pandas>=1.3.0
