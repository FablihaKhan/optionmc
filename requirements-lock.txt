# Fully pinned environment, verified end-to-end on 2026-08-19
# (Windows 11, CPython 3.12.10): 12/12 tests, 4/4 examples, both CLI methods.
#
# Reproduce it with:
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements-lock.txt
#   .venv/Scripts/python -m pip install -e ./optionmc
#
# Use requirements-dev.txt instead when you deliberately want newer releases.

# --- direct dependencies -------------------------------------------------
numpy==2.5.2
scipy==1.18.0
matplotlib==3.11.1
seaborn==0.13.2
pandas==3.0.5

# --- test tooling --------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.15.4

# --- transitive ----------------------------------------------------------
Pygments==2.21.0
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
iniconfig==2.3.0
kiwisolver==1.5.0
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.3

# --- extension additions, verified 2026-08-20 ---------------------------
# The pins above reproduce the BASE OptionMC package and are unchanged by
# anything below: installing these left every version above exactly as it was.
#
# Only direct dependencies are pinned here. Streamlit pulls in roughly forty
# transitive packages of its own; pinning all of them would turn this file into
# something nobody maintains, and none of them touch a number the project
# reports.
yfinance==1.6.0
streamlit==1.62.0
plotly==6.9.0
